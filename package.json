{
  "name": "schema-safe-kafkajs",
  "version": "0.0.5",
  "description": "Package for safe publishing messages into kafka topic with schema validation",
  "main": "./dist/index.js",
  "homepage": "https://www.google.com",
  "repository": {
    "url": "https://github.com"
  },
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "deploy": "npm run build && npm publish"
  },
  "keywords": [
    "schema",
    "kafka",
    "kafkajs",
    "avro",
    "avro-schema",
    "schema-registry"
  ],
  "author": "Pablo Izquierdo",
  "license": "MIT",
  "devDependencies": {
    "tsup": "^8.0.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@kafkajs/confluent-schema-registry": "^3.3.0",
    "kafkajs": "^2.2.4"
  }
}
